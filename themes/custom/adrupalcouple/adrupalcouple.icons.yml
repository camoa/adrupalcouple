# UI Icons: OFF (Q-09 committed).
# The React template uses only Unicode characters (☰ ☀ ☾) rendered as aria-hidden text.
# No UI Icons pack registered. Add a pack here only if an icon field is introduced later.
