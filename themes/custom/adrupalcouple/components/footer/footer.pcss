/* footer.pcss — A Drupal Couple footer SDC */
/* All color tokens (text-muted, base-200, base-300, base-content) are defined in p2_tokens. */
/* All typography tokens (font-heading) are defined in p2_tokens. */
/* No component-private overrides required. */
/* If a footer-specific adjustment is ever needed, add it here (not in global styles). */
